<!-- Added by codex-title-sync -->
When starting work in this Git project or worktree, and after switching branches or worktrees, run:
`codex-title-sync enable`

If unavailable, continue and report that chat title sync was skipped.

--- project-doc ---

CRITICAL: Before asking the user to reopen a broad or cross-service architecture question, consult `design-docs/architectural-decision-log.md`. If the topic is logged as resolved, follow the canonical location named there; if the docs appear to conflict, report the conflict instead of re-asking the settled question. When changing architecture, update each touched boundary's owning doc named by the log. `design-docs/architectural-decision-log.md`

CRITICAL: V1 architecture drafting lives under top-level `v1-draft-architecture/`, outside the current V0 `design-docs/` folder. Treat those docs as future, non-authoritative, and not deployable for V0. Do not use V1 drafts to implement, scope, unblock, or deploy V0 unless the user explicitly asks to promote a V1 decision and the owning V0 doc plus `design-docs/architectural-decision-log.md` are updated. `v1-draft-architecture/README.md`
