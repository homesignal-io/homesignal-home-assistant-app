import React from "react";
import { createRoot } from "react-dom/client";
import HomeSignalProductSkeleton from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HomeSignalProductSkeleton />
  </React.StrictMode>
);
